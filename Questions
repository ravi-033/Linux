1. What are the most commonly used commands in your project?
ls, cp, mv, scp, rm, mkdir, touch, vi, df, du -sh, find, grep

2. print errors from remote log?
curl <URL> | grep -i 'error'

3. Print only process id of particular process?
ps -ef | grep 'process_name' | awk -F" " '{print $2}'

4. Write a shell script to print numbers divide by 3 & 5 but not by 15?
#!/bin/bash

for i in {1..100}; do
    if (( (i % 3 == 0 || i % 5 == 0) && i % 15 != 0 )); then
        echo $i
    fi
done


5. What is crontab in linux?
Crontab helps in scheduling tasks automatically. 

6. How will you debug shell scrpt?
set -x

7. how will you open a file in read-only mode?
vim -R <file_name>
less <file_name>
cat <file_name>

8. Difference between hard and soft link?
ln original.txt hardlink.txt   - In hardlink, if the original file gets deleted, hardlink file will be present (copy).
ln -s original.txt softlink.txt - In softlink, if the original file gets deleted, softlink file gets invalid.


 Write a simple shell script to list all processes 
 Write a script to print only errors from a remote log
 Write a shell script to print numbers divided by 3 & 5 and not 15
 Write a script to print number of "S" in Mississippi
 How will you debug the shell script?
 What is crontab in Linux? Can you provide an example of usage?
 How to open a read-only file?
 What is a difference between soft and hard link?
 What is a difference between break and continue statements ?
 What are some disadvantages of Shell scripting?
 What a different types of loops and when to use?
 Is bash dynamic or statically typed and why?
 Explain about a network troubleshooting utility?
 How will you sort list on names in a file ?
 How will you manage logs of a system that generate huge log files everyday?
